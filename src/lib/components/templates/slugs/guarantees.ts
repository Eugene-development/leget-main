import { guaranteesMap } from '../Promo-1/pages/guarantees/map';
import type { ComponentMapsByTemplate } from '../types';

/** Карты блоков «/guarantees» по шаблонам — только Promo-1. См. partnership.ts. */
export const guaranteesMaps: ComponentMapsByTemplate = { 1: guaranteesMap };
