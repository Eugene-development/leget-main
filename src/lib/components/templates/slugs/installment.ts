import { installmentMap } from '../Promo-1/pages/installment/map';
import type { ComponentMapsByTemplate } from '../types';

/** Карты блоков «/installment» по шаблонам — только Promo-1. См. partnership.ts. */
export const installmentMaps: ComponentMapsByTemplate = { 1: installmentMap };
