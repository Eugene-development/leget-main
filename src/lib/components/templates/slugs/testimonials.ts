import { testimonialsMap } from '../Promo-1/pages/testimonials/map';
import type { ComponentMapsByTemplate } from '../types';

/** Карты блоков «/testimonials» по шаблонам — только Promo-1. См. partnership.ts. */
export const testimonialsMaps: ComponentMapsByTemplate = { 1: testimonialsMap };
