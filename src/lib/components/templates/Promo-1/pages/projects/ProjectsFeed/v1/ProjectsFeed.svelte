<script lang="ts">
	// Артикул: 1.26.2.1 — см. docs/architecture/component-articles-map.md
	//
	// Вариант 1: лента открывается СПИСКОМ. Дальше режим выбирает посетитель
	// переключателем в шапке ленты, и его выбор запоминается.
	//
	// Тело ленты общее у обеих версий и живёт в ../Feed.svelte — версии
	// отличаются ровно этим пропсом. Так и должно читаться: если завтра они
	// начнут расходиться сильнее, расхождение появится здесь явно, а не
	// расползётся по двум копиям одинаковой разметки.
	import Feed from '../Feed.svelte';

	let { data = $bindable() }: { data: Record<string, unknown> } = $props();
</script>

<Feed bind:data defaultView="list" />
