import type { ComponentMap } from '../../../types';
import Hero from './Hero.svelte';

/** Блоки страницы «О компании» (Test). Источник один — см. Promo-1/pages/about/map.ts. */
export const aboutMap: ComponentMap = {
	Hero
};
