<script lang="ts">
	/**
	 * Каталог намеренно не наследует layout сайта: карточка должна содержать блок
	 * и ничего кроме блока. Ни хедера, ни футера, ни баннера.
	 */
	let { children } = $props();
</script>

{@render children()}
